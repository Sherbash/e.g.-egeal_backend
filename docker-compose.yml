version: '3.8'

services:
  marshall-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: marshall-server  
    container_name: marshall-backend_neuro_night_ai
    ports:
      - "5300:5300"
    env_file:
      - .env
    restart: unless-stopped
